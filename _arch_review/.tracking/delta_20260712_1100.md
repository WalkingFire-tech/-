# 🟢 巡检#66 Delta 报告：代谢编排器落地 + subprocess 硬化

> 生成: 2026-07-12 11:00 | HEAD: afc344d | 评分: 89 (→持平, 35轮天花板🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥)

## 变更摘要

| 方面 | 状态 | 细节 |
|------|------|------|
| 新 commit | ❌ | HEAD 仍为 afc344d |
| 工作区源文件变更 | 🟢 **14 文件** +60 行 net | subprocess 硬化 11 文件 + 代谢编排器 + parallel_router 重构 |
| 新 untracked 路径 | 🟡 **6** | core/instinct/, core/learning.py, docs/AUTOPOIETIC_ARCHITECTURE.md, 3 delta |
| 新增裸 except | 🟢 **0** (HEAD) | metabolism.py 13 except 全部 `except Exception` ✅ |
| 新增 sqlite3.connect | 🟢 **0** | metabolism.py 使用 DatabaseManager ✅ |
| 新模块加入 core/ | 🟢 `core/instinct/` (267 行) | 代谢编排器 Phase 1 |

## 核心变更分析

### 1. subprocess 硬化（跨 11 文件，-0/+22 行）

所有涉及 `subprocess.run()` 的调用增加 `creationflags=subprocess.CREATE_NO_WINDOW`。

**文件清单**：
- `backend/services/parallel_router.py`, `ollama_path.py`
- `core/capability_creation_loop.py`, `closed_loop_module.py`, `learning/capability_gap_learner.py`, `resource_awareness/health_monitor.py`, `self_evolution.py`, `tools/bash_tool.py`
- `infrastructure/code_executor.py`, `hardware_monitor.py`
- `start_smart.py`

**影响**：Windows 平台不再弹出 CMD 控制台窗口。用户体验改善。属于 SpiritCore「追求本质」原则。

### 2. 🆕 代谢编排器（`core/instinct/`）

```
core/instinct/
├── __init__.py         → 16 行，导出 MetabolismOrchestrator
└── metabolism.py       → 251 行，编排循环实现
```

**循环流程**：ingest（摄入）→ digest（消化）→ grow（生长）→ shed（排泄）
- ingest → 调用 `layered_memory` 同步新经验
- digest → 调用 `gap_growth` 理解需求
- grow → 调用 `sleep_consolidation` 巩固记忆
- shed → 调用 `knowledge_forgetting` 清理过时

**集成方式**：
- `infrastructure/scheduled_tasks.py`: +19 行，`_job_metabolism` 每 5 分钟 tick
- 自适应节拍：`_assess_load()` → idle 时完整循环，busy 时快速摄入，peak 时跳过

**质量**：
- 13 处 `except Exception`（0 处裸 except ✅）
- 使用 `DatabaseManager`（0 处 sqlite3.connect ✅）
- 单例模式（`__new__` 实现），线程安全考虑 `_lock` 在后续版本

**对齐 AUTOPOIETIC_ARCHITECTURE.md**：直接响应 [留言] 2026-07-12 建议①「代谢编排器可立即实施——风险最低、增量而不伤现有架构」。

### 3. parallel_router 慢路径取消重构（-12/+10 行）

**变更**：5 处 `asyncio.ensure_future(_background_collect(...))` → `t.cancel()`

以前的做法会在自我推理质量足够高时，仍然把慢路径任务放到后台继续运行（资源泄漏）。现在直接 cancel 掉不再需要的任务。

**SpiritCore 对齐**：「三思后行」— 不遗留未管理的后台任务。

### 4. state_collector schema 兼容（+4 行）

新增 PRAGMA 检测：如果 `state_reports` 表已存在但缺少 `layer` 列，DROP 后重建。

**SpiritCore 对齐**：「失败有方向」— 迁移失败时有安全回退。

### 5. frontend 版本跃升（+1/-1 行）

v3.5.0 → v4.0.0。版本号对齐项目的 v4.0.0 架构阶段。

## 持续风险

| 风险 | 等级 | 说明 |
|------|------|------|
| core/learning.py 回归 | 🟡 中等 | 847 行，19 except（均 Exception ✅），与 enhanced_learning.py 重复 |
| chat_orchestrator 2344 行 | 🟡 中等 | 超健康线 4.7 倍，工作区 2534 行（+190 未在 commit 中） |
| 天花板 35 轮 | 🔴 高 | 所有跟踪指标满分，需扩围维度才能突破 |
| 测试覆盖 14/100 | 🔴 高 | 持续无改善 |

## 与架构路线图对齐

| 文档 | 对齐度 | 说明 |
|------|--------|------|
| AUTOPOIETIC_ARCHITECTURE.md | ✅ **高度** | 代谢编排器 Phase 1 落地 |
| SpiritCore 原则 | ✅ **全部** | 6 条原则在本次变更中有体现 |
| PHASE2_ARCHITECTURE.md | ✅ **一致** | 代谢编排器属于 Phase 2「认知整合」范围 |
| [留言] 2026-07-12 | ✅ **已回复** | 两条建议均已评估，代谢编排器已实装 |
