# 🚨 架构变更 Delta 报告

> **触发**: 巡检#6 — 2026-07-07 11:25
> **触发条件**: P0 目标文件变更 + SpiritCore 原则违反 + 架构级别重构

---

## 1. 变更全景

### 核心文件行数变化（自 HEAD a041f49）

| 文件 | HEAD | 当前 | 净变化 | 变化率 | 评估 |
|------|------|------|--------|--------|------|
| `chat_stream.py` | 2750 | 2378 | **-372** | -13.5% | ✅ 正向缩减 |
| `main_fast.py` | 1672 | 2350 | **+678** | **+40.5%** | 🔴 严重膨胀 |

### 核心文件行数变化（自上次巡检 08:30）

| 文件 | 上轮 | 当前 | 变化 | 信号 |
|------|------|------|------|------|
| `chat_stream.py` | 2133 | 2378 | **+245** | 🔴 回弹 |
| `main_fast.py` | 2160 | 2350 | **+190** | 🔴 继续增长 |

---

## 2. P0 目标文件变更分析

### chat_stream.py — import 重构进行中

**变更性质**: 架构重构（refactor）

**HEAD→当前变更**:
- 老的内联定义（`_check_vector_available()`, 条件导入）被移除
- 新增 9 个 path_handlers 模块的 import 别名
- 新增 intent_service 和 response_aggregator 的 import

**架构影响评估**:
- ✅ 方向正确：将职能函数迁移到 `backend/services/path_handlers/`
- ⚠️ 过渡期膨胀：import 别名 + 尚存的旧函数定义共存导致行数增加
- ⚠️ `_shared.py` (92行) 作为公共依赖，可能成为新的"垃圾抽屉"

**行数趋势**: HEAD 2750 → 前次 2133 → 当前 2378
```
2750 ──→ 2133 ──→ 2378
 HEAD     08:30    现在
  ↓-617    ↑+245
  缩减22.4%  回弹11.5%
```

### main_fast.py — 纯功能新增，零提取

**变更性质**: 功能新增（feature）

**新增内容**:
1. 日志框架（loguru 轮转/压缩/编码）
2. `_periodic_hardware_log()` 后台任务（30秒间隔写入硬件日志）
3. 事件总线集成（proactivity 订阅 + idle 周期处理）

**架构影响评估**:
- 🔴 **违反"借道还债"原则**: 净增 541 行，没有带走任何一个旧函数
- 🔴 **裸 except 33 处未动**: 新增代码中 `except Exception: pass` 在 `_periodic_hardware_log()` 中出现
- ⚠️ 这些功能更应放在独立模块（如 `infrastructure/` 或 `middleware/`）而不堆在 main_fast.py

**行数趋势**: HEAD 1672 → 前次 2160 → 当前 2350
```
1672 ──→ 2160 ──→ 2350
 HEAD     08:30    现在
  ↑+488    ↑+190
  膨胀29.2%  继续增长8.8%
```

---

## 3. SpiritCore 违反明细

### 违反项 1: "失败有方向" — main_fast.py 33 处裸 except

**状态**: 🔴 连续 6 轮未改善

裸 `except:` 的作用域（main_fast.py）：
- 行 256-277: 批量资源初始化块（8 处）
- 行 526-587: 路由处理器（10 处）
- 行 634-816: 系统功能注册（8 处）
- 行 956-1542: 业务逻辑（7 处）

**修复路径**: 每轮巡检至少替换 5 处 `except:` → `except Exception as e: logger.warning(...)`，6 轮可清零。

### 违反项 2: "追求本质" — 核心文件回弹增长

**状态**: 🟡 趋势转弱

chat_stream.py 的 import 重构方向正确但过渡期膨胀；
main_fast.py 纯粹是策略执行偏差（加功能前未提取旧代码）。

**纠正措施**: main_fast 下一轮必须至少提取一个路由到 `routers/` 目录，否则将继续拉低评分。

---

## 4. 健康评分趋势

```
42 ──→ 58 ──→ 54 ──→ 54 ──→ 56 ──→ 54 ← 首次下降
基线    #2    #3    #4    #5    #6 (当前)
```

**连续 3 次下降警示**: 目前不是连续下降（上次↑+2），但趋势需要密切关注。

---

## 5. 行动推荐

| 优先级 | 行动 | 预期影响 |
|--------|------|----------|
| P0 | main_fast.py 提取 health/proactivity/event_bus 到独立模块 | 缩减 200-300 行 + 改善耦合 |
| P0 | chat_stream.py 清理 import 重构后的重复定义 | 缩减 150-200 行，恢复缩减趋势 |
| P1 | 裸 except 替换 5 处（本次→下轮） | 异常指标↑+2 |
| P1 | DatabaseManager 启动首个文件迁移 | DB 指标首次突破 0 |
| P2 | 提交工作区分批入库 | 降低冲突风险 |

---

## 6. 已删除/休眠模块清单

| 模块 | 状态 | 影响 |
|------|------|------|
| `core/vector_retriever.py` | ✅ 已物理删除 | 移至 infrastructure/vector_retriever.py |
| `infrastructure/fact_store_v2.py` | ✅ 已物理删除 | 功能合并至 fact_store.py |
| `infrastructure/versioned_fact_store.py` | ✅ 已物理删除 | 版本管理合并至 fact_store.py |
