# v3.7.0-M51 会话归档 — Ollama身份修复 + CBNR-AGI 2.2 + 世界模型充实

## 日期
2026-07-06

## 核心目标
完成M49/M50所有待做项，从"质量深化"推进到"智能进化"

## 完成清单

### P0: Ollama模型身份问题 ✅
- **根因**：`/api/generate`端点的`system`参数对qwen2.5-coder:7b无效
- **修复**：所有Ollama调用从`/api/generate`改为`/api/chat`，使用messages格式
- **改动文件**：chat_handler.py, chat_stream.py, task_queue.py, metacognitive_executor.py, executor_agent.py, external_learner.py, never_give_up.py, persistent_tasks.py, ollama_adapter.py（共9个文件）
- **额外修复**：`_OLLAMA_MODEL_CACHE`变量定义被误删，已恢复
- **经验池清理**：删除1条旧"通义千问"身份回复

### P1: 经验池旧数据model_name补全 ✅
- 866条经验，0条NULL model_name
- 按intent_type推断：external_api→deepseek, ollama/ollama_candidate→ollama, metacognitive→ollama等

### P1: CBNR-AGI 2.2 — L1预测误差反馈到路径选择权重 ✅
- chat_stream中提取L1的attention_weights到cbnr_context（l1_prediction_error, l1_high_surprise, l1_focus_boost）
- `_compare_and_select`新增cbnr_ctx参数
- 高预测误差时：深度推理路径(Ollama/DeepSeek)评分×surprise_boost，经验池路径评分×(2-surprise_boost)
- 日志：高预测误差时输出"CBNR L1: 高预测误差(X.XX), 增强深度推理权重"

### P1: L3搜索树与world_model联动 ✅
- OrchestratorAgent.explore()新增world_model调用
- 从world_model获取实体关联节点，生成world_model_hypothesis类型假设
- 假设source字段保留原始来源（causal_model/counterfactual_model/world_model）

### P2: 世界模型因果图充实 ✅
- 新增10个节点：偏差检测、注意力权重、搜索树、棘轮门控、经验池、精神内核、同行者、存在层、主动感知、贡献归因
- 新增15条边：偏差检测→注意力权重(ENABLES)、注意力权重→搜索树(CAUSES)等
- 总计：27节点47边（原17节点32边）

### P2: GPU监控pyadl ✅
- 已确认pyadl正常工作，GPU监控正常（Radeon RX 580 Series）

### P2: RAM检测不一致 ✅
- 31.9GB是正确的系统RAM，8.0GB是GPU VRAM
- 当前API返回正确值

## 端到端验证结果
- `/api/chat` (非流式): ✅ 200, 身份正确显示"联盟拓荒者"
- `/api/chat/stream` (流式): ✅ 200, step/result事件正常
- 所有API端点: ✅ 8个端点全部200
- GPU: 53°C / 0% / normal模式
- RAM: 31.9GB / 41.6%使用

## 修改文件清单
```
backend/chat_handler.py                               # _SYSTEM_PROMPT+_OLLAMA_MODEL_CACHE恢复+Ollama改/api/chat
backend/chat_stream.py                                # Ollama改/api/chat+L1预测误差提取+_compare_and_select增加cbnr_ctx
core/task_queue.py                                    # Ollama改/api/chat
core/metacognitive_executor.py                        # Ollama改/api/chat
core/agents/executor_agent.py                         # Ollama改/api/chat
core/external_learner.py                              # Ollama改/api/chat
core/never_give_up.py                                 # Ollama改/api/chat
core/persistent_tasks.py                              # Ollama改/api/chat
adapters/llm/ollama_adapter.py                        # Ollama改/api/chat
core/cbnr/cognitive_residual.py                       # L3 Orchestrator+world_model联动
```

## 待做（按优先级）
1. **spirit_core进一步语义级验证** — 当前仍为关键词+正则，可考虑轻量语义相似度
2. **Ollama /api/chat兼容性** — 部分旧模型可能不支持messages格式，需fallback
3. **经验池质量分提升** — 当前866条经验，可批量重新评估quality_score
4. **CBNR-AGI 2.3** — L2注意力权重驱动的动态压缩比