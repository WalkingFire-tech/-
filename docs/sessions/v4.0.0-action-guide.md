# v4.0.0 行动指南 — 认知驱动执行 + 学习回路闭环

> 更新时间：2026-07-11 | 阶段：从"替系统修工具"到"让系统学会造工具"

---

## 一、核心愿景

**从"告诉你怎么做"到"直接帮你做"** — 系统必须能真正执行操作类请求，且认知层的理解必须驱动执行层的行动，而不是理解完被丢弃、执行层重新盲猜。

## 二、核心原则（不可违背）

1. **永不放弃** — 问题没有解决不了的，只有想不想得到方法
2. **追求本质** — 从第一性原理出发
3. **先规划，再更新行动指南，然后按步骤执行**
4. **代码验证靠运行而非推理**
5. **三思后行** — 先理解全景再审视局部，先搜索既有讨论再行动，先根因思考再修复

## 三、元宪法铁律

- R1: 未经沙盒验证的真谛，视同毒药
- R2: 未经渐进注入的重组，视同自杀
- R3: 未经人类允许的进化，视同背叛
- R4: **七维自检** — 行动前必须通过：①方向一致 ②看板衔接 ③最小侵入 ④无过度设计 ⑤治标+治本 ⑥可验证 ⑦精神内核对齐

## 四、行动前自检流程（R4强制）

每次修改代码或提出方案前，必须完成：

1. **理解全景** — 架构、哲学、看板历史、路线图阶段，不盯着局部
2. **搜索既有讨论** — 看板/路线图/架构分析，不重新发明轮子
3. **根因思考** — 问题是孤立故障还是未落地的架构原则？
4. **标本兼治** — 热修复(P0)与架构方案分离
5. **七维自检** — 全部通过才行动
6. **最优判断** — 依赖链排序，不做的事也明确列出

## 四、用户关键指令

- **start.bat必须是最终启动方式** — 每次修改完毕后确保start.bat可用
- **不要杀进程重启服务** — Windows上`--reload`不可用，需手动重启
- **文档只是一个建议** — 可以自由行动，按最稳妥最优的方式执行
- **任何人改 chat_stream.py 时，在 commit message 末尾加 [chat_stream] 标记
- **文档是实时更新的** — 每次执行前必须重新读取PHASE2_ARCHITECTURE.md和留言板
- **API是手段不是目的** — 系统先用自己的能力解决问题，API只是补充
- **不达目的不罢休** — 在未达到用户真正目的之前，不接受半成品输出
- **知识库不能忘** — 有用的东西必须写入knowledge_base
- **行动指南必须更新** — 每次总结后更新行动指南到docs/sessions/
- **留言板内容先规划再执行** — 看到有意义的内容，先规划放入行动指南再做

## 五、v4.0.0 已完成的关键修复

### 5.1 工具执行链三层修复

| # | 问题 | 根因 | 修复文件 |
|---|------|------|----------|
| 1 | tool_intent截断serial_port | other_tools[:3]排除了hint工具 + params变量引用顺序错误 | `tool_path.py` |
| 2 | 非流式API无工具调用 | chat_handler.py只有Ollama路径 | `chat_handler.py` |
| 3 | Windows空闲睡眠杀进程 | 电源管理60分钟空闲后睡眠 | `start.bat` |

### 5.2 自我推理对操作类问题降权

- 文件：`orchestrator_helpers.py:303`，self_reason对操作类quality上限40

### 5.3 工具结果质量提升

- 文件：`response_aggregator.py:39`，工具来源+实际数据额外+20分

### 5.4 前端分栏重构

- 对话框（左）+ 思考执行显示框（右），可拖动分隔线
- 文件：`index.html`, `styles.css`, `app.js`

### 5.5 学习回路接线（~50行代码闭环909行能力模块）

| 断裂 | 问题 | 修复 | 文件 |
|------|------|------|------|
| ① | ToolBuilder构建后不注册 | build_tool()成功后自动注册AutoToolWrapper | `tool_builder.py` |
| ② | 成熟技能不注册为工具 | _update_skill()成熟时调用_register_mature_skill() | `skill_emergence.py` |
| ③ | plan_tools()空时不查技能表 | S-2回退逻辑 | `tool_registry.py` |
| ④ | 能力创造回路成功后不通知ToolBuilder | _register_tool()中调用builder.record_success() | `capability_creation_loop.py` |

### 5.6 认知驱动执行修复（methodology流过整条执行链路）

| 修复 | 问题 | 方案 | 文件 |
|------|------|------|------|
| ① | methodology在fetch_tool_results中被丢弃 | 新增methodology参数传入 | `parallel_router.py` |
| ② | extract_tool_params无法理解"串口8" | methodology优先：domain=="硬件"时从数字提取COM端口 | `tool_path.py` |
| ③ | plan_tools不知道认知层理解的领域 | methodology.domain指导工具优先级（硬件+25分） | `tool_registry.py` |
| ④ | query_needs_tools与CognitiveDispatcher重复判断 | methodology.strategy=="tool_first"优先 | `parallel_router.py` |

### 5.7 chat_handler.py裸except清零

- 3处`except:` → `except Exception:`，chat_handler.py裸except归零

### 5.8 database is locked根治

- `database_manager.py`：读操作(query/query_one)不再加线程锁(WAL支持并发读)
- 所有操作统一3次retry+退避(executemany/executescript/query/query_one)

### 5.9 隐身搜索修复

- `web_search_tool.py`：snippet为空时用title兜底
- `stealth_search.py`：百度搜索增加更多CSS选择器回退+通用文本提取

### 5.10 历史记录丢失修复

- `chat_history.py`：从`db._get_conn()`改为`db.execute(commit=True)`/`db.query()`
- 根因：迁移时遗漏`conn.commit()`，新消息停留在事务缓冲区

### 5.11 CognitiveDispatcher硬件意图

- 新增`hardware`意图类型(串口/serial/硬件/设备/端口等)
- `_scan_capabilities_fast`从tool_registry读取已注册工具
- hardware意图走slow路径

### 5.12 意图-产出对照验证

- `chat_orchestrator.py`：自我验证前检查意图与产出是否匹配
- 硬件类：用户要读数据但返回扫描结果→降置信度到0.3

## 六、系统运行环境

- **Windows本地机器**，AMD Ryzen 5 3600X + 32GB RAM + RX 580 8GB VRAM
- pyserial已安装，SerialPortTool已注册
- **COM3 GPS设备**：USB-SERIAL CH340, 9600/8/N/1, 可读取真实NMEA数据（注意：COM号可能变化）
- Ollama模型：gemma-4-12B, qwen2.5-coder:7b, deepcoder
- 服务器启动需要约45-60秒
- **已禁用Windows空闲睡眠**

## 七、已知问题与待办

### 高优先级

- [ ] 服务器偶尔崩溃（可能是内存或Ollama推理导致，需进一步排查）
- [ ] infrastructure/下`db._get_conn()`直接操作需逐步迁移到db.query()（76处commit已修，连接方式待迁移）
- [ ] ToolBuilder锻造后沙箱验证增强（当前exec()执行无安全隔离）

### 中优先级

- [ ] 隐身搜索(baidu)的snippet提取仍可能失败（CSS选择器可能再次过时）
- [ ] S-3: CognitivePlanner.process()接入主路由（风险高，需渐进式接入）
- [ ] closed_loop_orchestrator状态机异常路径可能卡住

### 低优先级

- [ ] S1: ToolRegistry双注册表统一（core/tool_registry.py + tools/registry.py 接口不兼容，需单独设计迁移路径）
- [ ] 引入集成度/自我模型/端口覆盖新评分维度
- [ ] SelfModel聚合各学习模块形成统一能力画像

### ✅ 本轮已完成（v4.0.0 Phase 3.5 认知管道契约化）

| 修复项 | 文件 | 改动 |
|--------|------|------|
| P0: ExperienceAbstractor接入反思阶段 | `chat_orchestrator.py` | 7步闭环"抽象"层补入 |
| P0: S3 quality_score KeyError | `metacognitive_executor.py` | 超时默认dict补全 |
| P0: S6 consolidate()方法不存在 | `sleep_consolidation.py` | 新增公共方法 |
| P1: S7 SpiritCore debug→error | `spirit_core.py` 5处 | logger.debug→logger.error |
| P1: CognitiveDispatchResult契约 | `cognitive_dispatcher.py` | TypedDict定义 |
| P1: never_give_up单例统一 | `never_give_up.py` | get_cognitive_dispatcher() |
| P1: closed_loop_orchestrator裸except | `closed_loop_orchestrator.py` 10处 | except→except Exception |
| S4: 悬空代码4方法 | `metacognitive_executor.py` | 删除_store_experience等4个未调用方法 |
| Step0: clarifying_questions | `closed_loop_orchestrator.py` | 删除字段+引用 |
| M6: 状态机冗余赋值 | `closed_loop_orchestrator.py` | 删除4处ctx.state=自身 |
| M13: SpiritCore绕过DatabaseManager | `spirit_core.py` 5处 | _db_connect→_db+execute/query |

### ✅ 本轮已完成（v4.0.0 Phase 4 认知架构"思维回路"改造 + 增量）

| # | 修复项 | 文件 | 改动要点 |
|---|--------|------|---------|
| 1 | P0: ExperienceAbstractor接入反思阶段 | `chat_orchestrator.py` | 7步闭环"抽象"层补入 |
| 2 | P0: S3 quality_score KeyError | `metacognitive_executor.py` | 超时默认dict补全quality_score等字段 |
| 3 | P0: S6 consolidate()方法不存在 | `sleep_consolidation.py` | 新增consolidate()公共方法返回dict |
| 4 | P1: S7 SpiritCore debug→error | `spirit_core.py` 5处 | logger.debug→logger.error |
| 5 | P1: CognitiveDispatchResult契约 | `cognitive_dispatcher.py` | TypedDict定义 |
| 6 | P1: never_give_up单例统一 | `never_give_up.py` | get_cognitive_dispatcher() |
| 7 | P1: closed_loop_orchestrator裸except | `closed_loop_orchestrator.py` 10处 | except→except Exception |
| 8 | S4: 悬空代码4方法删除 | `metacognitive_executor.py` | 删除_retrieve_metacognitive_examples等4个未调用方法 |
| 9 | Step0: clarifying_questions删除 | `closed_loop_orchestrator.py` | 删除字段+引用 |
| 10 | M6: 状态机冗余赋值 | `closed_loop_orchestrator.py` | 删除4处ctx.state=自身 |
| 11 | M13: SpiritCore绕过DatabaseManager | `spirit_core.py` 5处 | _db_connect→_db+execute/query |
| 12 | Bug: 质疑检测死循环 | `chat_orchestrator.py` | 找不到历史时降级为complex_query |
| 13 | Bug: get_last_response裸连接 | `experience_path.py` 3处 | db._get_conn()→db.query() |
| 14 | Bug: GPS时间UTC→北京 | `serial_port_tool.py` | 新增北京时间字段 |
| 15 | infrastructure/ 76处commit缺失 | 27个infrastructure文件 | 自动添加conn.commit() |
| 16 | Bug: challenge截胡hardware | `cognitive_dispatcher.py` | hardware优先级提升到challenge之前 |
| 17 | Bug: LLM伪造硬件数据 | `chat_orchestrator.py` + `chat_handler.py` | 伪造检测+System Prompt约束 |
| 18 | Bug: 海拔显示不完整 | `serial_port_tool.py` | MSL+WGS84+大地水准面差距+说明 |
| 19 | 串口只扫描不读取 | `serial_port_tool.py` | 含"读取"关键词时自动查找USB串口 |
| 20 | 工具结果95分提前取消 | `parallel_router.py` | has_tool_result_95时取消慢路径 |
| 21 | ToolBuilder LLM合成 | `tool_builder.py` | _generate_basic_tool调用LLM推理工具代码 |
| 22 | persistent_solver构建后执行 | `persistent_solver.py` | build_tool后立即执行返回结果 |

### ✅ 认知架构Phase 1-3（"思维回路"改造）

| # | 改造 | 文件 | 做了什么 |
|---|------|------|---------|
| 23 | Phase1: skill_emergence本能触发 | `skill_emergence.py` | 新增reflex_query()+_extract_skeleton()+automation_level+confidence升级逻辑 |
| 24 | Phase2: experience_abstractor骨架抽象 | `experience_abstractor.py` | 新增_extract_skeleton()+find_analogous()骨架匹配 |
| 25 | Phase3: chat_orchestrator能力缺口检测 | `chat_orchestrator.py` | 本质闸门后加"本能查询→骨架联想→能力缺口检测"三步 |

### ✅ 增量改造（PheromoneField + MentalModelLibrary提取）

| # | 改造 | 文件 | 做了什么 |
|---|------|------|---------|
| 26 | 对话连续性感知 | `chat_orchestrator.py` | _perceive_continuity()检测话题漂移/指代消解/上下文衰减，信号注入methodology |
| 27 | 气味特征(scent)增强 | `experience_abstractor.py` | SCENT_VOCAB词汇表+extract_scent()+scent_similarity()，骨架匹配升级为骨架60%+气味40%综合评分 |

## 八、关键文件速查

### 本次修改的核心文件

```
# 认知架构Phase 1-3（思维回路改造）
core/skill_emergence.py                        # reflex_query()+_extract_skeleton()+automation_level+confidence升级
core/cognition/experience_abstractor.py        # _extract_skeleton()+find_analogous()+SCENT_VOCAB+extract_scent()+scent_similarity()
backend/services/chat_orchestrator.py           # 能力缺口检测+本能查询+骨架联想+质疑降级+伪造检测+抽象层接入+对话连续性感知

# 工具执行链修复
core/tools/serial_port_tool.py                 # 北京时间+海拔MSL/WGS84+自动查找USB串口+HDOP警告
core/cognitive_dispatcher.py                   # hardware>challenge优先级+TypedDict契约
core/learning/tool_builder.py                  # LLM合成工具代码+_synthesize_tool_code()
backend/services/persistent_solver.py          # 构建后立即执行

# 数据库commit修复
core/spirit_core.py                            # _db_connect→_db+execute/query + debug→error
infrastructure/（27个文件）                     # 76处conn.commit()缺失修复

# 其他修复
core/metacognitive_executor.py                 # quality_score默认值+悬空代码删除
core/closed_loop_orchestrator.py               # 裸except+冗余state+clarifying_questions删除
core/never_give_up.py                          # 单例统一
core/presence/sleep_consolidation.py           # consolidate()公共方法
backend/services/parallel_router.py             # 工具结果95分提前取消慢路径
backend/services/path_handlers/experience_path.py  # _get_conn→db.query
backend/chat_handler.py                        # System Prompt加"严禁编造数据"
```

### 三大文件夹

```
knowledge_base/                    # 有用的东西放进去不要忘
docs/sessions/                     # 行动指南（每次总结后更新）
_arch_review/.tracking/            # 留言板（先规划再执行）
```

## 九、留言板关键发现

### 9.1 学习回路断裂（巡检#41，已修复 ✅）

909行能力模块各自为政，50行接线代码闭环。详见5.5节。

### 9.2 认知驱动断裂（巡检#43后架构巡检员留言，已修复 ✅）

EssenceReasoner理解了"串口8=串口数据读取"，但methodology在传递中被丢弃，tool_path只能重新用正则理解。详见5.6节。

### 9.3 工作区冻结（巡检#42~#43，待提交 🔴）

- 已冻结11+轮，24个源文件变更+13个新文件
- "代码只有提交了才算数"
- 建议分2-3批提交

### 9.4 S-3 CognitivePlanner（仍跳过）

- cp.process()是同步阻塞方法，直接插入异步路由可能阻塞
- 按R2原则需渐进式接入
