# v3.7.0-M50 会话归档 — 质量深化 + Bug修复

## 日期
2026-07-06

## 核心目标
从"功能建设"转向"质量深化"——让每一层都真正活起来

## 完成清单

### P0: Bug修复
- **`/api/chat` 500错误** — `chat_handler.py`中`current_model = _select_model()`引用了不存在的函数名，改为`current_model = _get_available_ollama_model() or "unknown"`
- **精神内核误判"自相矛盾"** — 移除容易误判的矛盾对("不能/能"、"否/是")，改为按句子级别检测而非距离30字符

### P1: chat_stream Hub统一管道确认
- 确认当前分散调用模式(process_l1/l2/l3 + finalize_distributed)是正确的架构选择
- chat_stream需要在L1/L2/L3之间插入大量业务逻辑(9路径并行处理)，不能用简单的process()统一管道
- finalize_distributed()在L3之后正确触发棘轮门控和统计

### P1: L2实体提取增强
- 新增领域实体模式：CBNR/AGI/SSE/REST/HTTP/JSON等协议和架构术语
- 新增中文领域名词：偏差/不确定性/预测编码/注意力权重/因果链/反事实/搜索树/工作记忆/棘轮/门控/适应度/经验池/精神内核/同行者/存在层/感知层/执行层/元认知层等
- 新增中文通用名词块提取：2-8字中文词组，过滤停用词
- 修复【】括号匹配（原来是`【([^】]+)」`，改为`【([^】]+)】`）
- 实体上限从5提升到8

### P1: L1偏差检测增强
- availability_heuristic阈值从0.6降到0.4，增加更多触发词("之前说的"/"我之前"/"上次说的"/"刚说的"/"刚才说的"/"earlier")
- sunk_cost_fallacy阈值从0.6降到0.4，增加更多触发词("已经做了"/"已经花了"/"已经投入"/"白费了"/"浪费了"/"不能白费")

### P1: L3搜索树持久化确认
- 已验证SQLite持久化正常工作（2个节点已保存并恢复）
- 每5次process自动保存

### P2: L3 Critic验证增强
- 新增语义重复检测：同一假设内容（前30字符）不重复通过
- 新增因果假设质量检查：中文内容词<2且置信度<0.5的因果假设被拒绝
- 新增反事实合理性检查：以"实现"开头且含"前提条件"且置信度<0.5的被拒绝

### P2: spirit_core语义级验证
- 维度1增强：回复>30字但缺乏解释性关键词（因为/由于/所以/因此/意味着/说明/表明/通过/利用/基于/根据）且<80字时判定为无意义
- 维度6增强：回复>50字但缺乏深层分析关键词（本质/核心/关键/根本/原因/原理/机制/底层/基础/逻辑）且行数≤2且<60字时判定为表面
- 维度7增强：检测"总之/总的来说/简而言之/总而言之"后跟过度简化结论的模式
- 维度8增强：检测"一定/必然/绝对/肯定"后跟"是/对/正确/没错"的绝对化断言（回复<60字时）

### P2: active_perception信号驱动行动
- `_handle_signal`新增传递给存在层：调用`existence_layer.receive_perception_signal()`
- `_handle_signal`新增传递给任务队列：高置信(>0.7)的NEED_EMERGENCE和EMOTION_SHIFT信号提交perception_driven任务
- 存在层新增`receive_perception_signal`方法：emotion_shift/need_emergence信号(置信度>0.6)触发AWAKE状态切换，silence_break信号(置信度>0.7)触发AWAKE

## 端到端验证结果
- `/api/chat` (非流式): ✅ 200, 正常响应
- `/api/chat/stream` (流式): ✅ 200, step/result/resource_conservative事件正常
- CBNR三层处理: ✅ L1偏差检测+L2压缩+L3搜索树持久化
- 精神内核验证: ✅ 不再误判"自相矛盾"，验证通过
- 所有API端点: ✅ 6个端点全部200
- GPU: 52°C / 23% / conservative模式
- RAM: 14.96GB可用 / 53.2%使用

## 修改文件清单
```
backend/chat_handler.py                               # current_model函数名修复
core/spirit_core.py                                   # 逻辑自洽按句检测+维度1/6/7/8语义级增强
core/cbnr/cognitive_bottleneck.py                     # L2实体提取增强(领域实体+中文名词块+修复括号)
core/cbnr/cognitive_normalization.py                  # L1偏差检测阈值降低+触发词扩充
core/cbnr/cognitive_residual.py                       # L3 Critic验证增强(重复检测+因果质量+反事实合理性)
core/presence/active_perception.py                    # 信号驱动: 传递给存在层+任务队列
core/presence/existence_layer.py                      # 新增receive_perception_signal方法
```

## 待做（按优先级）
1. **Ollama模型身份问题** — qwen2.5-coder:7b返回"通义千问"自我介绍，需在system prompt中注入系统身份
2. **经验池旧数据model_name补全** — 511条unknown+314条None可尝试从intent_type推断
3. **CBNR-AGI 2.2** — L1预测误差反馈到路径选择权重
4. **L3搜索树与world_model联动** — L3的Orchestrator应调用world_model反事实
5. **GPU监控pyadl模块** — 安装或改用其他GPU监控方式
6. **RAM检测不一致排查** — 31.9GB vs 8.0GB
7. **spirit_core进一步语义级验证** — 当前仍为关键词+正则，可考虑轻量语义相似度