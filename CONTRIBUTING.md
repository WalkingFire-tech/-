# 贡献指南

感谢你对联盟拓荒者的关注！本项目遵循以下贡献规范。

## 开发环境

- Python 3.10+
- Windows 本地开发（AMD Ryzen 5 3600X + 32GB RAM + RX 580 8GB VRAM）
- 启动方式：`start.bat`（轮询 `/api/health` 等待就绪后打开浏览器）

## 核心原则

1. **永不放弃** — 问题没有解决不了的
2. **追求本质** — 从第一性原理出发
3. **先读代码再行动** — 不基于目录名猜测，必须读实际代码验证
4. **代码验证靠运行而非推理**
5. **三思后行** — 先理解全景再审视局部
6. **不替系统做决定，让系统自己做决定**
7. **不新建模块，增强现有模块**
8. **先模块测试，再端到端测试**

## 元宪法铁律

- **R1**: 未经沙盒验证的真谛，视同毒药
- **R2**: 未经渐进注入的重组，视同自杀
- **R3**: 同行者的进化始于本心。路径须可回溯，偏移须可感知，不可逆变更须共决

## 提交规范

- 修改 `chat_stream.py` 时，commit message 末尾加 `[chat_stream]` 标记
- 修改 `main_fast.py` 时，commit message 末尾加 `[main_fast]` 标记
- commit message 用简体中文或英文均可，重点描述"为什么"而非"做了什么"

## 代码风格

- 不添加注释（除非被要求）
- 遵循现有代码风格和导入约定
- 不引入项目未使用的第三方库（先检查 `requirements.txt` 或 `pyproject.toml`）
- 异常处理使用 `except Exception:` 而非裸 `except:`
- 数据库访问统一使用 `DatabaseManager`，不直接使用 `sqlite3.connect`

## 安全

- 不提交 `.env`、密钥、凭证等敏感信息
- NAS 相关信息不得出现在任何文件中
- 进化岛注入必须遵循6步安全协议（propose→sandbox→1pct→20pct→100pct）+ 回滚

## 测试

- 修改完代码先验证模块测试通过，再进行端到端测试
- 服务器需要重启才能使修改生效（Windows 上 `--reload` 不可用）