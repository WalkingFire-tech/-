# 🚀 快速启动指南

## 方式1: 一键启动测试（推荐）⭐

**双击运行**:
```
运行测试.bat
```

这个脚本会：
1. ✅ 检查Python环境
2. ✅ 启动后端服务
3. ✅ 自动测试所有API
4. ✅ 打开前端界面

---

## 方式2: 分步启动

### 步骤1: 启动后端
```powershell
# 在项目根目录
python -m uvicorn api:app --host 0.0.0.0 --port 8000
```

等待看到：
```
INFO:     Application startup complete.
```

### 步骤2: 测试API
```powershell
python test_running_backend.py
```

### 步骤3: 访问前端
浏览器打开: http://localhost:8000/

---

## 方式3: 使用启动脚本

```powershell
.\启动.bat
```

---

## 📋 可用脚本列表

| 脚本 | 用途 |
|------|------|
| `运行测试.bat` | **一键启动+测试（推荐）** |
| `启动.bat` | 一键启动（无测试） |
| `diagnose.bat` | 诊断问题 |
| `quick_test.bat` | 快速API测试 |
| `test_running_backend.py` | 测试已运行的后端 |
| `auto_test.py` | Python自动化测试 |

---

## ✅ 验证成功

如果看到以下输出，说明启动成功：

```
测试: 健康检查 (/api/health)
  ✓ 响应: {"status": "ok", "version": "3.1.1", ...}

测试: 统计信息 (/api/stats)
  ✓ 响应: {"experiences": 59, "active_rules": 2, ...}

测试: 模型列表 (/api/models)
  ✓ 响应: {"models": [...]}

测试: 前端页面 (/)
  ✓ 状态码: 200

通过: 4/4
🎉 所有测试通过！
```

---

## 🌐 访问地址

启动成功后：

| 地址 | 说明 |
|------|------|
| http://localhost:8000/ | **前端界面** |
| http://localhost:8000/docs | API文档 |
| http://localhost:8000/api/health | 健康检查 |

---

## 🐛 如果启动失败

### 检查Python版本
```powershell
python --version  # 需要 3.11+
```

### 检查依赖
```powershell
pip install -r requirements.txt
```

### 检查端口占用
```powershell
netstat -ano | findstr :8000
```

### 查看详细日志
运行 `diagnose.bat` 查看完整错误信息

---

## 📝 常见问题

**Q: Ollama连接失败？**
A: 这是正常的，系统会自动使用Mock适配器。不影响使用。

**Q: 页面无法访问？**
A: 确保看到 "Application startup complete" 后再访问。

**Q: 端口被占用？**
A: 运行 `taskkill /PID <进程ID> /F` 结束占用进程。

---

**🎯 推荐使用 `运行测试.bat` 一键启动并测试！**